
apply plugin: 'com.android.application'

android {
    namespace "com.example.youtube_notes_app"
    compileSdkVersion 33

    defaultConfig {
        applicationId "com.example.youtube_notes_app"
        minSdkVersion 21
        targetSdkVersion 33
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    implementation 'com.google.android.material:material:1.4.0'
}
